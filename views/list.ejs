<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Flights - Flight Manager</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #2c3e50, #34495e);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
        }
        .nav {
            background: #f8f9fa;
            padding: 15px 30px;
            border-bottom: 1px solid #e9ecef;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .nav a {
            color: #667eea;
            text-decoration: none;
            padding: 8px 16px;
            border-radius: 5px;
            transition: background 0.3s;
        }
        .nav a:hover {
            background: #e9ecef;
        }
        .search-section {
            padding: 30px;
            background: #f8f9fa;
            border-bottom: 1px solid #e9ecef;
        }
        .search-container {
            display: flex;
            gap: 15px;
            max-width: 600px;
            margin: 0 auto;
        }
        .search-input {
            flex: 1;
            padding: 15px 20px;
            border: 2px solid #e9ecef;
            border-radius: 50px;
            font-size: 1rem;
            outline: none;
        }
        .search-input:focus {
            border-color: #667eea;
        }
        .search-btn {
            padding: 15px 30px;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 50px;
            cursor: pointer;
        }
        .content {
            padding: 30px;
        }
        .results-info {
            margin-bottom: 20px;
            color: #6c757d;
            font-size: 1.1rem;
        }
        .flights-grid {
            display: grid;
            gap: 20px;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
        }
        .flight-card {
            border: 1px solid #e9ecef;
            border-radius: 10px;
            padding: 20px;
            background: white;
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .flight-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }
        .flight-header {
            display: flex;
            justify-content: between;
            align-items: center;
            margin-bottom: 15px;
        }
        .flight-number {
            font-size: 1.3rem;
            font-weight: bold;
            color: #2c3e50;
        }
        .flight-status {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: bold;
        }
        .status-on-time { background: #d4edda; color: #155724; }
        .status-delayed { background: #fff3cd; color: #856404; }
        .status-cancelled { background: #f8d7da; color: #721c24; }
        .flight-details {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-bottom: 15px;
        }
        .detail-item {
            display: flex;
            flex-direction: column;
        }
        .detail-label {
            font-size: 0.8rem;
            color: #6c757d;
            margin-bottom: 2px;
        }
        .detail-value {
            font-size: 0.9rem;
            font-weight: 500;
            color: #2c3e50;
        }
        .flight-actions {
            display: flex;
            gap: 10px;
            margin-top: 15px;
        }
        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 0.9rem;
            transition: background 0.3s;
        }
        .btn-view {
            background: #28a745;
            color: white;
        }
        .btn-edit {
            background: #ffc107;
            color: black;
        }
        .btn-delete {
            background: #dc3545;
            color: white;
        }
        .btn:hover {
            opacity: 0.9;
        }
        .no-results {
            text-align: center;
            padding: 60px 20px;
            color: #6c757d;
            grid-column: 1 / -1;
        }
        .highlight {
            background-color: yellow;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>✈️ Flight Manager</h1>
            <p>Search and Manage Your Flights</p>
        </div>

        <div class="nav">
            <div>
                <a href="/list">All Flights</a>
                <a href="/search" style="background: #667eea; color: white;">Search</a>
                <a href="/api-test">API Test</a>
            </div>
            <div>
                <span>Welcome, <%= user.name %>!</span>
                <a href="/logout" style="margin-left: 15px;">Logout</a>
            </div>
        </div>

        <div class="search-section">
            <form action="/search" method="GET" class="search-container">
                <input 
                    type="text" 
                    name="q" 
                    class="search-input" 
                    placeholder="Search by flight number, destination, airline, airport, status..."
                    value="<%= searchTerm %>"
                >
                <button type="submit" class="search-btn">🔍 Search</button>
            </form>
        </div>

        <div class="content">
            <div class="results-info">
                <% if (searchTerm) { %>
                    Found <%= resultsCount %> flights matching "<%= searchTerm %>"
                <% } else { %>
                    Showing all <%= resultsCount %> flights
                <% } %>
            </div>

            <div class="flights-grid">
                <% if (flights.length === 0) { %>
                    <div class="no-results">
                        <h3>No flights found</h3>
                        <p>Try different search terms or <a href="/list">view all flights</a></p>
                    </div>
                <% } else { %>
                    <% flights.forEach(flight => { %>
                        <div class="flight-card">
                            <div class="flight-header">
                                <div class="flight-number"><%= flight.flightNumber %></div>
                                <div class="flight-status status-<%= flight.status.toLowerCase().replace(' ', '-') %>">
                                    <%= flight.status %>
                                </div>
                            </div>
                            
                            <div class="flight-details">
                                <div class="detail-item">
                                    <span class="detail-label">Airline</span>
                                    <span class="detail-value"><%= flight.airline %></span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">Destination</span>
                                    <span class="detail-value"><%= flight.destination %></span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">Departure</span>
                                    <span class="detail-value"><%= flight.departureAirport %></span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">Arrival</span>
                                    <span class="detail-value"><%= flight.arrivalAirport %></span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">Time</span>
                                    <span class="detail-value"><%= flight.hours %>:<%= flight.minutes.toString().padStart(2, '0') %></span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">Gate</span>
                                    <span class="detail-value"><%= flight.gate %></span>
                                </div>
                            </div>

                            <div class="flight-actions">
                                <a href="/details?_id=<%= flight._id %>" class="btn btn-view">View</a>
                                <a href="/edit?_id=<%= flight._id %>" class="btn btn-edit">Edit</a>
                                <form action="/flights/<%= flight.flightNumber %>?_method=DELETE" method="POST" style="display: inline;">
                                    <button type="submit" class="btn btn-delete" onclick="return confirm('Delete flight <%= flight.flightNumber %>?')">Delete</button>
                                </form>
                            </div>
                        </div>
                    <% }); %>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
