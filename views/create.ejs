<!DOCTYPE html>
<html>
<head>
  <title>Create Flight</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="header">
    <h2>Create a new flight object</h2>
    <p class="user-info">
      Welcome, <strong><%= user.name %></strong> 
      <span class="user-id">(ID: <%= user.id %>)</span> 
      | <a href="/">Home</a> 
      | <a href="/logout">Logout</a>
    </p>
  </div>

  <hr>

  <form action="/create" method="POST" enctype="multipart/form-data">
    FlightNumber:<br><input type="text" name="flightNumber" required><br>
    Destination:<br><input type="text" name="destination" required><br>
    Time:<br>
    <select name="hours" required>
      <% for(let i=0; i<24; i++) { %>
        <option value="<%= i.toString().padStart(2,'0') %>"><%= i.toString().padStart(2,'0') %></option>
      <% } %>
    </select> :
    <select name="minutes" required>
      <% for(let i=0; i<60; i+=5) { %>
        <option value="<%= i.toString().padStart(2,'0') %>"><%= i.toString().padStart(2,'0') %></option>
      <% } %>
    </select><br>
    Gate:<br>
    <select name="gate" required>
      <% for(let c of 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('')) { %>
        <option><%= c %></option>
      <% } %>
    </select><br>
    Status:<br>
    <select name="status" required>
      <option>Cancelled</option><option>On Time</option><option>Delayed</option>
      <option>Departed</option><option>Arrived</option><option>Holding</option>
    </select><br><br>
    Photo: <input type="file" name="filetoupload"><br><br>
    <input type="submit" value="Submit">
  </form>
</body>
</html>
