<!DOCTYPE html>
<html>
<head>
  <title>Edit Flight</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="card shadow">
      <div class="card-body">
        <h2>Edit Flight: <%= flight.flightNumber %></h2>
        <p class="text-muted">Welcome, <%= user.name %> (ID: <%= user.id %>) | <a href="/list">Dashboard</a></p>
        <hr>

        <!-- 重要：唔好 submit _id 畀 MongoDB！ -->
        <form action="/flights/<%= flight._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
          
          <div class="row g-3 mb-3">
            <div class="col-md-6">
              <label>Flight Number</label>
              <input name="flightNumber" class="form-control" value="<%= flight.flightNumber %>" required>
            </div>
            <div class="col-md-6">
              <label>Destination</label>
              <input name="destination" class="form-control" value="<%= flight.destination %>" required>
            </div>
            <div class="col-md-3">
              <label>Hour</label>
              <select name="hours" class="form-select" required>
                <% for(let i=0; i<24; i++) { let h=i.toString().padStart(2,'0') %>
                  <option value="<%= h %>" <%= flight.hours==h ? 'selected' : '' %>><%= h %></option>
                <% } %>
              </select>
            </div>
            <div class="col-md-3">
              <label>Minute</label>
              <select name="minutes" class="form-select" required>
                <% for(let i=0; i<60; i+=5) { let m=i.toString().padStart(2,'0') %>
                  <option value="<%= m %>" <%= flight.minutes==m ? 'selected' : '' %>><%= m %></option>
                <% } %>
              </select>
            </div>
            <div class="col-md-6">
              <label>Gate</label>
              <input name="gate" class="form-control" value="<%= flight.gate %>">
            </div>
            <div class="col-md-6">
              <label>Status</label>
              <select name="status" class="form-select">
                <option <%= flight.status==='On Time' ? 'selected' : '' %>>On Time</option>
                <option <%= flight.status==='Delayed' ? 'selected' : '' %>>Delayed</option>
                <option <%= flight.status==='Cancelled' ? 'selected' : '' %>>Cancelled</option>
              </select>
            </div>
            <div class="col-12">
              <label>Replace Photo</label>
              <input type="file" name="filetoupload" class="form-control" accept="image/*">
            </div>
          </div>

          <button type="submit" class="btn btn-primary">Update Flight</button>
          <a href="/list" class="btn btn-secondary">Cancel</a>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
